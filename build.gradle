// Top-level build file
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.6.1'
        // Removed Bintray plugin — it's dead
        classpath 'com.github.dcendents:android-maven-gradle-plugin:2.1' // works better with JitPack
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' }
    }
}

